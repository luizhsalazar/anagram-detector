<%= form_for(@anagram) do |f| %>
  <% if @anagram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anagram.errors.count, "error") %> prohibited this anagram from being saved:</h2>

      <ul>
      <% @anagram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sentence %><br>
    <%= f.text_area :sentence %>
  </div>
  <div class="field">
    <%= f.label :is_anagram %><br>
    <%= f.check_box :is_anagram %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
